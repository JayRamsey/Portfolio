#include <stdio.h>
#include <math.h>


int main(){

    /*
    sqrt(9) returns 3
    pow(2, 4) = 2^8
    round(3.14) = 3 (int)
    ceil(arg) rounds up
    floor(arg) truncates
    fabs(-100) = 100 (absolute)
    log()
    sin cos and tan
    */


    return 0;
}